# PalindromeCheckerApp
## Use Case 4 – Character Array Based Palindrome Check
This version converts the string into a character array and uses the two-pointer 
technique to check if it is a palindrome.
- Converts string to char[]
- Uses start and end pointers
- Compares characters efficiently without extra objects
- Data Structure: char[]